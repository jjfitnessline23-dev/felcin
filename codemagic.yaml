workflows:

  ios-app-store:
    name: Felcin iOS - App Store
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      groups:
        - ios
      xcode: latest
      node: latest
      cocoapods: default
      vars:
        BUNDLE_ID: com.felcin.app
        XCODE_WORKSPACE: ios/App/App.xcworkspace
        XCODE_SCHEME: App

    scripts:

      - name: Build static web bundle
        script: |
          set -e
          git clone https://github.com/jjfitnessline23-dev/felcin-next.git /tmp/felcin-next
          cd /tmp/felcin-next
          npm install --legacy-peer-deps

          python3 << 'PYEOF'
          import os, re

          for root, dirs, files in os.walk('app/api'):
              for f in files:
                  if f == 'route.ts':
                      p = os.path.join(root, f)
                      c = open(p).read()
                      nc = c.replace("'force-dynamic'","'force-static'").replace('"force-dynamic"','"force-static"')
                      if nc != c: open(p,'w').write(nc)

          dynamic_routes = [
              ('app/(protected)/challenges/[id]/page.tsx','id'),
              ('app/(protected)/ghost/[id]/page.tsx','id'),
              ('app/(protected)/live/[id]/page.tsx','id'),
              ('app/(protected)/podcasts/[id]/page.tsx','id'),
              ('app/(protected)/podcasts/live/[id]/page.tsx','id'),
              ('app/(protected)/subscribe/[uid]/page.tsx','uid'),
              ('app/(protected)/tag/[name]/page.tsx','name'),
              ('app/(protected)/trainers/[uid]/page.tsx','uid'),
              ('app/(protected)/training-sessions/[sessionId]/page.tsx','sessionId'),
              ('app/(public)/run/live/[shareId]/page.tsx','shareId'),
          ]
          for path, key in dynamic_routes:
              if os.path.isfile(path):
                  open(path,'w').write(
                      'import { Suspense } from "react";\nimport PageClient from "./PageClient";\n\n'
                      'export function generateStaticParams() { return [{ '+key+': "_" }]; }\n'
                      'export const dynamicParams = false;\n\n'
                      'export default function Page() { return <Suspense fallback={null}><PageClient /></Suspense>; }\n'
                  )

          suspense_pages = [
              'app/(protected)/advertise/page.tsx',
              'app/(protected)/badges/page.tsx',
              'app/(protected)/comments/page.tsx',
              'app/(protected)/creator/page.tsx',
              'app/(protected)/earnings/page.tsx',
              'app/(protected)/explore/page.tsx',
              'app/(protected)/page.tsx',
              'app/(protected)/private-chats/page.tsx',
              'app/(protected)/training-sessions/page.tsx',
          ]
          for page in suspense_pages:
              if os.path.isfile(page):
                  cp = os.path.join(os.path.dirname(page),'page.content.tsx')
                  os.rename(page, cp)
                  open(page,'w').write(
                      'import { Suspense } from "react";\nimport PageContent from "./page.content";\n'
                      'export default function Page() { return <Suspense fallback={null}><PageContent /></Suspense>; }\n'
                  )

          up = 'app/(protected)/user-profile/page.tsx'
          if os.path.isfile(up):
              content = open(up).read()
              content = re.sub(r'export async function generateMetadata\b.*?\n\}','',content,flags=re.DOTALL)
              content = re.sub(r'async function fetchUserProfile\b.*?\n\}','',content,flags=re.DOTALL)
              content = content.replace('import type { Metadata } from "next";\n','')
              if 'Suspense' not in content:
                  content = content.replace('import PageClient from "./PageClient";',
                      'import { Suspense } from "react";\nimport PageClient from "./PageClient";')
              content = content.replace('return <PageClient />;','return <Suspense fallback={null}><PageClient /></Suspense>;')
              open(up,'w').write(content)
          PYEOF

          NEXT_PUBLIC_CAPACITOR_BUILD=true \
          NEXT_PUBLIC_API_URL="https://www.felcin.com" \
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY:-pk_test_51TeacHFq0Nd4lLkFwHZ7nutFk0jI0H4bmtffnaI92is7ym5KEVuXmppnVMgQhC9qP39zBhgzOukNYUE4yvtiwa0I00CycqtIbo}" \
          npm run build

          [ -f "out/index.html" ] || { echo "ERROR: static build failed"; exit 1; }
          echo "Pages: $(find out -name '*.html' | wc -l)"

          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe 's|fetch\("/api/|fetch("https://www.felcin.com/api/|g'
          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe "s|fetch\\('/api/|fetch('https://www.felcin.com/api/|g"
          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe 's|\`/api/|\`https://www.felcin.com/api/|g'

      - name: Patch capacitor.config.json
        script: |
          python3 << 'PYEOF'
          import json
          with open('capacitor.config.json') as f:
              c = json.load(f)
          c['appId'] = 'com.felcin.app'
          c['webDir'] = '/tmp/felcin-next/out'
          c.pop('server', None)
          c.setdefault('ios', {})
          c['ios'].pop('scheme', None)
          c['ios']['limitsNavigationsToAppBoundDomains'] = False
          with open('capacitor.config.json','w') as f:
              json.dump(c, f, indent=2)
          print('capacitor.config.json: bundled mode')
          PYEOF

      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
          npm install @capacitor-firebase/firestore --legacy-peer-deps 2>/dev/null || true
          npm install -g @capacitor/cli

      - name: Add iOS platform (fresh)
        script: |
          rm -rf ios
          npx cap add ios

      - name: Set deployment target to 16.0
        script: |
          python3 << 'PYEOF'
          import re
          path = 'ios/App/Podfile'
          content = open(path).read()
          content = re.sub(r"platform :ios, '[0-9.]+'", "platform :ios, '16.0'", content)
          open(path,'w').write(content)
          PYEOF

      - name: Pod install
        script: cd ios/App && pod install

      - name: Capacitor sync
        script: npx cap sync ios

      - name: Configure GoogleService-Info.plist and Info.plist
        script: |
          cp GoogleService-Info.plist ios/App/App/GoogleService-Info.plist

          gem install xcodeproj --quiet 2>/dev/null || true
          ruby << 'RUBYEOF'
          require 'xcodeproj'
          project = Xcodeproj::Project.open('ios/App/App.xcodeproj')
          app_group = project.main_group['App']
          unless app_group.find_file_by_path('GoogleService-Info.plist')
            ref = app_group.new_file('GoogleService-Info.plist')
            ref.last_known_file_type = 'text.plist.xml'
            project.targets.first.resources_build_phase.add_file_reference(ref)
            project.save
          end
          RUBYEOF

          REVERSED_CLIENT_ID=$(/usr/libexec/PlistBuddy -c "Print :REVERSED_CLIENT_ID" ios/App/App/GoogleService-Info.plist)
          export REVERSED_CLIENT_ID

          python3 << 'PYEOF'
          import os, plistlib
          plist_path = 'ios/App/App/Info.plist'
          with open(plist_path,'rb') as f:
              plist = plistlib.load(f)

          rid = os.environ['REVERSED_CLIENT_ID']
          url_types = [u for u in plist.get('CFBundleURLTypes',[]) if u.get('CFBundleURLName') != 'GoogleSignIn']
          url_types.append({'CFBundleURLName':'GoogleSignIn','CFBundleURLSchemes':[rid]})
          plist['CFBundleURLTypes'] = url_types

          plist['NSLocationWhenInUseUsageDescription'] = 'Felcin uses your location to track your running routes live on the map and save your PRs.'
          plist['NSLocationAlwaysAndWhenInUseUsageDescription'] = 'Felcin uses your location to track running routes and show your position on the map.'
          plist.setdefault('NSCameraUsageDescription', 'Felcin needs camera access to take photos and videos for posts and stories.')
          plist.setdefault('NSMicrophoneUsageDescription', 'Felcin needs microphone access to record audio for videos.')
          plist.setdefault('NSPhotoLibraryUsageDescription', 'Felcin needs photo library access to share photos and videos.')
          plist.setdefault('NSPhotoLibraryAddUsageDescription', 'Felcin needs permission to save photos and videos to your library.')
          plist['ITSAppUsesNonExemptEncryption'] = False

          with open(plist_path,'wb') as f:
              plistlib.dump(plist, f, fmt=plistlib.FMT_XML)
          print(f'Info.plist configured')
          PYEOF

      - name: Set app icon and black launch screen
        script: |
          cp ios-icons/*.png ios/App/App/Assets.xcassets/AppIcon.appiconset/
          cp ios-icons/Contents.json ios/App/App/Assets.xcassets/AppIcon.appiconset/Contents.json

          python3 << 'PYEOF'
          import re
          path = 'ios/App/App/Base.lproj/LaunchScreen.storyboard'
          content = open(path).read()
          content = re.sub(
              r'<color key="backgroundColor"[^/]*/?>',
              '<color key="backgroundColor" red="0.0" green="0.0" blue="0.0" alpha="1" colorSpace="custom" customColorSpace="sRGB"/>',
              content
          )
          open(path,'w').write(content)
          PYEOF

      - name: Patch ViewController.swift
        script: |
          cat > "ios/App/App/ViewController.swift" << 'SWIFTEOF'
          import UIKit
          import Capacitor
          import WebKit

          class ViewController: CAPBridgeViewController {

              override func viewDidLoad() {
                  super.viewDidLoad()
                  view.backgroundColor = UIColor.black
                  webView?.isOpaque = false
                  webView?.backgroundColor = UIColor.black
                  webView?.scrollView.backgroundColor = UIColor.black

                  // Block IndexedDB before ANY page JavaScript runs.
                  // @firebase/app heartbeat service calls indexedDB.open() on startup.
                  // On capacitor:// scheme + active network, this triggers a WKWebView bug
                  // that deadlocks ALL JS async callbacks (Promises, setTimeout) forever.
                  // WKUserScript at .atDocumentStart is injected by WebKit itself before
                  // any <script> tags execute — guaranteed to run first, cannot be blocked.
                  let blockIDB = WKUserScript(
                      source: """
                      (function() {
                        try {
                          var fakeReq = function() {
                            var r = new EventTarget();
                            r.result = null;
                            r.readyState = 'done';
                            r.onsuccess = null; r.onerror = null;
                            r.onblocked = null; r.onupgradeneeded = null;
                            r.error = new DOMException('IndexedDB blocked','SecurityError');
                            setTimeout(function(){
                              var e = new Event('error');
                              if (typeof r.onerror === 'function') r.onerror(e);
                              r.dispatchEvent(e);
                            }, 0);
                            return r;
                          };
                          if (window.IDBFactory) {
                            IDBFactory.prototype.open = fakeReq;
                            IDBFactory.prototype.deleteDatabase = fakeReq;
                          }
                          try { window.indexedDB.open = fakeReq; } catch(e) {}
                        } catch(e) {}
                      })();
                      """,
                      injectionTime: .atDocumentStart,
                      forMainFrameOnly: true
                  )
                  webView?.configuration.userContentController.addUserScript(blockIDB)
              }

              override func capacitorDidLoad() {
                  webView?.isOpaque = false
                  webView?.backgroundColor = UIColor.black
                  webView?.scrollView.backgroundColor = UIColor.black
              }

              @available(iOS 15.0, *)
              override func webView(_ webView: WKWebView, requestMediaCapturePermissionFor origin: WKSecurityOrigin, initiatedByFrame frame: WKFrameInfo, type: WKMediaCaptureType, decisionHandler: @escaping (WKPermissionDecision) -> Void) {
                  decisionHandler(.grant)
              }
          }
          SWIFTEOF

      - name: Patch AppDelegate.swift
        script: |
          cat > "ios/App/App/AppDelegate.swift" << 'SWIFTEOF'
          import UIKit
          import Capacitor
          import FirebaseCore

          @UIApplicationMain
          class AppDelegate: UIResponder, UIApplicationDelegate {
              var window: UIWindow?

              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  FirebaseApp.configure()
                  return true
              }

              func application(_ app: UIApplication, open url: URL, options: [UIApplication.OpenURLOptionsKey: Any] = [:]) -> Bool {
                  return ApplicationDelegateProxy.shared.application(app, open: url, options: options)
              }

              func application(_ application: UIApplication, continue userActivity: NSUserActivity, restorationHandler: @escaping ([UIUserActivityRestoring]?) -> Void) -> Bool {
                  return ApplicationDelegateProxy.shared.application(application, continue: userActivity, restorationHandler: restorationHandler)
              }
          }
          SWIFTEOF

      - name: iPhone only
        script: |
          perl -i -pe 's/TARGETED_DEVICE_FAMILY = "1,2";/TARGETED_DEVICE_FAMILY = 1;/g' ios/App/App.xcodeproj/project.pbxproj
          perl -i -pe 's/TARGETED_DEVICE_FAMILY = 1,2;/TARGETED_DEVICE_FAMILY = 1;/g' ios/App/App.xcodeproj/project.pbxproj

      - name: Set build number
        script: |
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" ios/App/App/Info.plist
          echo "Build: $BUILD_NUMBER"

      - name: Enable Google Sign-In subspec
        script: |
          perl -i -pe "s/default_subspec = 'Lite'/default_subspec = 'Google'/" \
            node_modules/@capacitor-firebase/authentication/CapacitorFirebaseAuthentication.podspec

      - name: Add Sign in with Apple entitlement
        script: |
          python3 << 'PYEOF'
          open("ios/App/App/App.entitlements","w").write("""<?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
              <key>com.apple.developer.applesignin</key>
              <array><string>Default</string></array>
              <key>com.apple.developer.associated-domains</key>
              <array>
                  <string>applinks:felcin.com</string>
                  <string>applinks:www.felcin.com</string>
              </array>
          </dict></plist>""".strip())
          PYEOF

          gem install xcodeproj --quiet 2>/dev/null || true
          ruby -e "
          require 'xcodeproj'
          p = Xcodeproj::Project.open('ios/App/App.xcodeproj')
          p.targets.each{|t| t.build_configurations.each{|c| c.build_settings['CODE_SIGN_ENTITLEMENTS']='App/App.entitlements'}}
          p.save
          "

      - name: Final pod install
        script: |
          cd ios/App
          rm -f Podfile.lock
          pod install --repo-update

      - name: Set up code signing
        script: |
          echo "$DIST_CERT_P12" | base64 --decode > /tmp/distribution.p12

          KEYCHAIN=/tmp/app-signing.keychain-db
          security create-keychain -p codemagic123 "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p codemagic123 "$KEYCHAIN"
          security import /tmp/distribution.p12 -k "$KEYCHAIN" -P "$DIST_CERT_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/xcodebuild
          security set-key-partition-list -S apple-tool:,apple: -s -k codemagic123 "$KEYCHAIN"
          security list-keychains -d user -s "$KEYCHAIN" ~/Library/Keychains/login.keychain-db

          PROFILES="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PROFILES"
          echo "$PROVISIONING_PROFILE" | base64 --decode > /tmp/profile.mobileprovision
          security cms -D -i /tmp/profile.mobileprovision -o /tmp/decoded.plist 2>/dev/null
          UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' /tmp/decoded.plist)
          cp /tmp/profile.mobileprovision "$PROFILES/$UUID.mobileprovision"
          echo "Profile: $UUID"

          PBXPROJ="ios/App/App.xcodeproj/project.pbxproj"
          perl -i -pe "s/CODE_SIGN_STYLE = Automatic;/CODE_SIGN_STYLE = Manual;\n\t\t\t\tPROVISIONING_PROFILE_SPECIFIER = \"$UUID\";/g" "$PBXPROJ"
          perl -i -pe 's/CODE_SIGN_IDENTITY = "iPhone Developer";/CODE_SIGN_IDENTITY = "Apple Distribution";/g' "$PBXPROJ"
          perl -i -pe 's/PRODUCT_BUNDLE_IDENTIFIER = ("?)app\.felcin\.social\1;/PRODUCT_BUNDLE_IDENTIFIER = "com.felcin.app";/g' "$PBXPROJ"

          cat > /tmp/export_options.plist << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>app-store-connect</string>
            <key>teamID</key><string>UWSVWR4VTX</string>
            <key>signingStyle</key><string>manual</string>
            <key>signingCertificate</key><string>Apple Distribution</string>
            <key>provisioningProfiles</key>
            <dict><key>com.felcin.app</key><string>$UUID</string></dict>
          </dict></plist>
          PLIST

      - name: Build IPA
        script: |
          set -eo pipefail
          ARCHIVE="build/ios/xcarchive/App.xcarchive"
          IPA_DIR="build/ios/ipa"
          mkdir -p "$(dirname $ARCHIVE)" "$IPA_DIR"

          xcodebuild \
            -workspace "$XCODE_WORKSPACE" -scheme "$XCODE_SCHEME" \
            -archivePath "$ARCHIVE" archive \
            COMPILER_INDEX_STORE_ENABLE=NO \
            DEVELOPMENT_TEAM=UWSVWR4VTX \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            MARKETING_VERSION=1.4 \
            CURRENT_PROJECT_VERSION=$BUILD_NUMBER

          xcodebuild -exportArchive \
            -archivePath "$ARCHIVE" \
            -exportPath "$IPA_DIR" \
            -exportOptionsPlist /tmp/export_options.plist

          echo "IPA: $(find $IPA_DIR -name '*.ipa' | head -1 | xargs ls -lh)"

      - name: Upload to TestFlight
        script: |
          IPA=$(find build/ios/ipa -name "*.ipa" | head -1)
          mkdir -p ~/private_keys
          echo "$APP_STORE_KEY_B64" | base64 --decode > ~/private_keys/AuthKey_${APP_STORE_CONNECT_KEY_IDENTIFIER}.p8
          xcrun altool --upload-app \
            --file "$IPA" --type ios \
            --apple-id 6763660775 \
            --apiKey "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID" \
            --output-format json

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: master
          include: true

    artifacts:
      - build/ios/ipa/*.ipa


  android-google-play:
    name: Felcin Android - Google Play
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      groups:
        - android
      java: 21
      node: latest
      vars:
        PACKAGE_NAME: com.felcin.app

    scripts:

      - name: Write Google Play credentials
        script: |
          set -e
          RAW=""
          for VAR_NAME in GOOGLE_PLAY_SERVICE_ACCOUNT_JSON GOOGLE_PLAY_SERVICE_ACCOUNT_CREDENTIALS; do
            VAR_VALUE="${!VAR_NAME}"
            [ -z "$VAR_VALUE" ] && continue
            if [ "${VAR_VALUE:0:1}" = "{" ]; then RAW="$VAR_VALUE"
            else
              DECODED=$(echo "$VAR_VALUE" | base64 --decode 2>/dev/null) || true
              [ "${DECODED:0:1}" = "{" ] && RAW="$DECODED"
            fi
            [ -n "$RAW" ] && break
          done
          [ -z "$RAW" ] && { echo "FATAL: No valid Google Play credentials"; exit 1; }
          echo "$RAW" > /tmp/gp-credentials.json

      - name: Build static web bundle
        script: |
          set -e
          git clone https://github.com/jjfitnessline23-dev/felcin-next.git /tmp/felcin-next
          cd /tmp/felcin-next
          npm install --legacy-peer-deps

          python3 << 'PYEOF'
          import os, re
          for root, dirs, files in os.walk('app/api'):
              for f in files:
                  if f == 'route.ts':
                      p = os.path.join(root, f)
                      c = open(p).read()
                      nc = c.replace("'force-dynamic'","'force-static'").replace('"force-dynamic"','"force-static"')
                      if nc != c: open(p,'w').write(nc)

          dynamic_routes = [
              ('app/(protected)/challenges/[id]/page.tsx','id'),
              ('app/(protected)/ghost/[id]/page.tsx','id'),
              ('app/(protected)/live/[id]/page.tsx','id'),
              ('app/(protected)/podcasts/[id]/page.tsx','id'),
              ('app/(protected)/podcasts/live/[id]/page.tsx','id'),
              ('app/(protected)/subscribe/[uid]/page.tsx','uid'),
              ('app/(protected)/tag/[name]/page.tsx','name'),
              ('app/(protected)/trainers/[uid]/page.tsx','uid'),
              ('app/(protected)/training-sessions/[sessionId]/page.tsx','sessionId'),
              ('app/(public)/run/live/[shareId]/page.tsx','shareId'),
          ]
          for path, key in dynamic_routes:
              if os.path.isfile(path):
                  open(path,'w').write(
                      'import { Suspense } from "react";\nimport PageClient from "./PageClient";\n\n'
                      'export function generateStaticParams() { return [{ '+key+': "_" }]; }\n'
                      'export const dynamicParams = false;\n\n'
                      'export default function Page() { return <Suspense fallback={null}><PageClient /></Suspense>; }\n'
                  )

          suspense_pages = [
              'app/(protected)/advertise/page.tsx','app/(protected)/badges/page.tsx',
              'app/(protected)/comments/page.tsx','app/(protected)/creator/page.tsx',
              'app/(protected)/earnings/page.tsx','app/(protected)/explore/page.tsx',
              'app/(protected)/page.tsx','app/(protected)/private-chats/page.tsx',
              'app/(protected)/training-sessions/page.tsx',
          ]
          for page in suspense_pages:
              if os.path.isfile(page):
                  cp = os.path.join(os.path.dirname(page),'page.content.tsx')
                  os.rename(page,cp)
                  open(page,'w').write(
                      'import { Suspense } from "react";\nimport PageContent from "./page.content";\n'
                      'export default function Page() { return <Suspense fallback={null}><PageContent /></Suspense>; }\n'
                  )

          up = 'app/(protected)/user-profile/page.tsx'
          if os.path.isfile(up):
              content = open(up).read()
              content = re.sub(r'export async function generateMetadata\b.*?\n\}','',content,flags=re.DOTALL)
              content = re.sub(r'async function fetchUserProfile\b.*?\n\}','',content,flags=re.DOTALL)
              content = content.replace('import type { Metadata } from "next";\n','')
              if 'Suspense' not in content:
                  content = content.replace('import PageClient from "./PageClient";',
                      'import { Suspense } from "react";\nimport PageClient from "./PageClient";')
              content = content.replace('return <PageClient />;','return <Suspense fallback={null}><PageClient /></Suspense>;')
              open(up,'w').write(content)
          PYEOF

          NEXT_PUBLIC_CAPACITOR_BUILD=true \
          NEXT_PUBLIC_API_URL="https://www.felcin.com" \
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY:-pk_test_51TeacHFq0Nd4lLkFwHZ7nutFk0jI0H4bmtffnaI92is7ym5KEVuXmppnVMgQhC9qP39zBhgzOukNYUE4yvtiwa0I00CycqtIbo}" \
          npm run build

          [ -f "out/index.html" ] || { echo "ERROR: static build failed"; exit 1; }

          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe 's|fetch\("/api/|fetch("https://www.felcin.com/api/|g'
          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe "s|fetch\\('/api/|fetch('https://www.felcin.com/api/|g"
          find out/_next/static/chunks -name '*.js' | xargs perl -i -pe 's|\`/api/|\`https://www.felcin.com/api/|g'

      - name: Patch capacitor.config.json
        script: |
          python3 << 'PYEOF'
          import json
          with open('capacitor.config.json') as f: c = json.load(f)
          c['webDir'] = '/tmp/felcin-next/out'
          c.pop('server', None)
          with open('capacitor.config.json','w') as f: json.dump(c, f, indent=2)
          PYEOF

      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
          npm install -g @capacitor/cli

      - name: Patch Google Sign-Out cache
        script: |
          perl -i -pe 's/public void signOut\(\) \{/public void signOut() {\n        if (mGoogleSignInClient != null) { mGoogleSignInClient.signOut(); }/' \
            "node_modules/@capacitor-firebase/authentication/android/src/main/java/io/capawesome/capacitorjs/plugins/firebase/authentication/handlers/GoogleAuthProviderHandler.java"

      - name: Add Android platform
        script: |
          if [ ! -f "android/gradlew" ]; then
            rm -rf android
            npx cap add android
          fi

      - name: Capacitor sync
        script: npx cap sync android

      - name: Set version and package name
        script: |
          BUILD_NO=$(( ${BUILD_NUMBER:-1} + 200 ))
          sed -i '' "s/versionCode [0-9]*/versionCode $BUILD_NO/" android/app/build.gradle
          sed -i '' "s/versionName \"[^\"]*\"/versionName \"2.0\"/" android/app/build.gradle
          sed -i '' "s/applicationId \"com\.felcin\.app\"/applicationId \"app.felcin.social\"/" android/app/build.gradle

      - name: Enable R8 optimization
        script: |
          perl -i -pe 's/minifyEnabled false/minifyEnabled true/' android/app/build.gradle
          perl -i -pe 's/shrinkResources false/shrinkResources true/' android/app/build.gradle
          grep -q 'shrinkResources' android/app/build.gradle || \
            perl -i -pe 's/minifyEnabled true/minifyEnabled true\n            shrinkResources true/' android/app/build.gradle
          perl -i -pe 's/proguard-android\.txt/proguard-android-optimize.txt/' android/app/build.gradle

      - name: Set app icons
        script: |
          if [ -d "android-icons" ]; then
            cp android-icons/mipmap-mdpi/*.png    android/app/src/main/res/mipmap-mdpi/
            cp android-icons/mipmap-hdpi/*.png    android/app/src/main/res/mipmap-hdpi/
            cp android-icons/mipmap-xhdpi/*.png   android/app/src/main/res/mipmap-xhdpi/
            cp android-icons/mipmap-xxhdpi/*.png  android/app/src/main/res/mipmap-xxhdpi/
            cp android-icons/mipmap-xxxhdpi/*.png android/app/src/main/res/mipmap-xxxhdpi/
          fi

      - name: Build AAB + APK
        script: |
          cd android
          chmod +x gradlew
          echo "$KEYSTORE_BASE64" | base64 --decode > /tmp/felcin-release.keystore
          ./gradlew bundleRelease assembleRelease \
            -Pandroid.injected.signing.store.file=/tmp/felcin-release.keystore \
            -Pandroid.injected.signing.store.password="$KEYSTORE_PASSWORD" \
            -Pandroid.injected.signing.key.alias="$KEY_ALIAS" \
            -Pandroid.injected.signing.key.password="$KEY_PASSWORD"

      - name: Publish to Google Play internal track
        script: |
          pip3 install --quiet google-auth google-api-python-client
          python3 - << 'PYEOF'
          from google.oauth2 import service_account
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload

          PACKAGE = 'app.felcin.social'
          AAB     = 'android/app/build/outputs/bundle/release/app-release.aab'
          creds   = service_account.Credentials.from_service_account_file(
              '/tmp/gp-credentials.json',
              scopes=['https://www.googleapis.com/auth/androidpublisher']
          )
          service = build('androidpublisher','v3',credentials=creds)
          edit    = service.edits().insert(packageName=PACKAGE,body={}).execute()
          eid     = edit['id']
          bundle  = service.edits().bundles().upload(
              packageName=PACKAGE, editId=eid,
              media_body=MediaFileUpload(AAB,mimetype='application/octet-stream',resumable=True)
          ).execute()
          vc = bundle['versionCode']
          service.edits().tracks().update(
              packageName=PACKAGE, editId=eid, track='internal',
              body={'releases':[{'versionCodes':[vc],'status':'completed'}]}
          ).execute()
          service.edits().commit(packageName=PACKAGE,editId=eid).execute()
          print(f'Published versionCode {vc} to internal track')
          PYEOF

    triggering:
      events: []

    artifacts:
      - android/app/build/outputs/bundle/release/app-release.aab
      - android/app/build/outputs/apk/release/app-release.apk
